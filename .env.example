# ─────────────────────────────────────────────────────────────
# FoC — Friend on Campus · Environment configuration (EXAMPLE)
# ─────────────────────────────────────────────────────────────
# Copy to `.env` and fill in real values:   cp .env.example .env
#
# `.env` is git-ignored — NEVER commit real secrets.
# Any variable read by any service MUST appear here with a safe
# placeholder so teammates know what to set.
#
# This is just an example; you can add more variables as needed.
# ─────────────────────────────────────────────────────────────

# ---- Global -------------------------------------------------
LOG_LEVEL=

# ---- Auth / JWT -----------
JWT_SECRET=
JWT_ACCESS_TOKEN_TTL=
JWT_REFRESH_TOKEN_TTL=

# ---- Postgres (single shared dev server, one DB per service) --
POSTGRES_USER=foc
POSTGRES_PASSWORD=changeme
POSTGRES_PORT=5432
USER_DB=user_dev
SUPPLIER_DB=supplier_dev

# ---- Service ports (host side) --------------------------------
# Containers listen on internal PORT=8080; these map to the host.
USER_PORT=8081
SUPPLIER_PORT=8082

# ---- Service DB URLs (as seen from inside compose network) ----
USER_DATABASE_URL=postgresql://foc:changeme@postgres:5432/user_dev?sslmode=disable
SUPPLIER_DATABASE_URL=postgresql://foc:changeme@postgres:5432/supplier_dev?sslmode=disable

# ---- Per-service runtime --------------------------------------
# PORT is read by cmd/server/main.go (defaults to 8080).
# Compose sets PORT=8080 internally; set PORT=8081/8082 for host runs.
PORT=
DATABASE_URL=
FIREBASE_CREDENTIALS_JSON=

# ---- Host-run services (only when running outside compose) ----
# The root Makefile builds host database URLs from the PostgreSQL settings above.
# Compose runs migrations automatically on service startup.

# ... Add more variables as needed
